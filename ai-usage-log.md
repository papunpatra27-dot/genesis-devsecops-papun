# AI Usage Log

Minimum 8 entries documenting AI tool usage across all tasks.
Format: Part | Tool | What I Asked | What AI Produced | What I Changed / Verified | Correct?

---

| Part | Tool | What I Asked | What AI Produced | What I Changed / Verified | Correct? |
|------|------|-------------|-----------------|--------------------------|----------|
| Task 1 (Terraform) | GitHub Copilot | Generate a Terraform networking module with VPC, public/private subnets, IGW, route tables, and a k3s-appropriate security group. Include VPC Flow Logs. | Complete main.tf with VPC, subnets, IGW, route table associations, security group, and Flow Log resources. Used aws_flow_log resource correctly. | Verified security group rules matched k3s port requirements (6443, 10250, 30000-32767). Added IMDSv2 enforcement note. Confirmed Flow Log IAM role had least-privilege actions. Changed `cidr_blocks` on SSH rule from `["0.0.0.0/0"]` to parameterised `var.admin_cidr` so production environments can restrict SSH. | Mostly — structure correct; security group needed tightening |
| Task 1 (Terraform) | GitHub Copilot | Write the IAM module for a GitHub Actions OIDC provider and least-privilege deployment role. Include the k3s EC2 node role scoped to ECR pull and CloudWatch Logs only. | OIDC provider, assume-role policy with sub condition, ECR push policy, and k3s node policy. The `thumbprint_list` was correctly populated. | Verified the OIDC condition used `StringLike` (not `StringEquals`) for the sub field so branch-level patterns work. Added `max_session_duration = 3600` to prevent long-lived tokens. Confirmed the k3s node role did NOT have ECR push permissions — it only had pull. | Yes — only minor tuning needed |
| Task 1 (Kyverno) | GitHub Copilot | Write three Kyverno ClusterPolicies in Enforce mode: no-root containers, require resource limits, ECR-only images. Use the foreach pattern for the image policy. | Three YAML files with correct apiVersion kyverno.io/v1, Enforce mode, and pattern-based validation. The foreach pattern was used for the ECR image check. | Verified the `validationFailureAction: Enforce` field (not Audit). Added `preconditions` to the init-container rules so they only fire when initContainers exist (avoids false-positive for pods with no init containers). Confirmed the ECR image pattern uses the correct registry format. | Mostly — preconditions were missing; added manually |
| Task 2 (App) | GitHub Copilot | Generate a FastAPI app with /health, POST /platform/units, GET /platform/units/{unit_id}. Add Prometheus instrumentation including a genesis_units_created_total counter with tenant_id label. | Complete main.py with all three endpoints, prometheus_client Counter and Histogram, middleware for per-request metrics, and Pydantic validation. | Verified the middleware correctly used `perf_counter()` not `time()` for latency. Confirmed the path normalisation logic collapses /platform/units/abc and /platform/units/xyz to the same label. Checked that the UNITS_CREATED_TOTAL counter incremented only on successful creates (not on 409 conflicts). Added the `not_empty` field validator which was missing from the original. | Mostly — field validator and path normalisation needed additions |
| Task 3 (GitHub Actions) | GitHub Copilot | Write a 13-stage CI/CD pipeline: stages 1-4 as a reusable workflow, stages 5-11 in ci.yml, stages 12-13 in deploy.yml. OIDC only — no AWS_ACCESS_KEY_ID. | Three workflow YAML files with correct `workflow_call` trigger, `needs` dependency chain, and OIDC authentication via `aws-actions/configure-aws-credentials`. | Verified that `needs: [security-scan]` on Stage 5 (docker-build) correctly blocks build if any of Stages 1-4 fails. Removed a line that had `AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}` in a comment (violates the OIDC-only requirement). Confirmed the `id-token: write` permission is on the job that signs images, not globally. | Mostly — had a commented-out AWS key reference that had to be removed |
| Task 3 (Argo Rollouts) | GitHub Copilot | Write an Argo Rollouts Rollout manifest with 5 canary steps (10% → pause → analysis → 25% → 50%) and a progressDeadlineSeconds of 600. | Valid Rollout YAML with canary steps, stableService/canaryService references, and progressDeadlineSeconds. The analysis step used the correct `templates` list format. | The AI omitted the `trafficRouting` section — added nginx ingress-based traffic splitting. Verified the `setWeight` values matched the required percentages. Added the `version: stable` label to the pod template for the Grafana canary vs stable panel to work. | Partially — trafficRouting section was missing; labels needed adding |
| Task 4 (PromQL / SLO) | GitHub Copilot | Write the PromQL expression for the canary AnalysisTemplate and SLO that measures the HTTP 5xx error rate on the canary service. Ensure it references the service label set by Argo Rollouts. | PromQL with `rate()` over 2m window, `status_code=~"5.."` filter, division of 5xx sum by total sum, templated service name via `{{args.service-name}}`. | Verified the query returns a value between 0 and 1 (not a percentage), matching the `successCondition: result[0] <= 0.02` assertion. Confirmed the metric name `http_requests_total` matches what the FastAPI app actually emits (not `flask_http_request_total` which would be a different framework). Aligned the same expression in both analysis-template.yaml and the SLO YAML. | Yes — expression was correct; needed cross-referencing verification |
| Task 5 (DR Runbook) | GitHub Copilot | Write a numbered command-level DR runbook for recovering a k3s cluster in a new AWS region after a regional failure. Include the exact Helm commands, the Argo CD bootstrap sequence, and a validation curl command. | 28-step numbered runbook with actual CLI commands including Terraform init, helm install, argocd login, and a Python smoke test assertion. | Verified that Kyverno is installed before the Argo CD sync step (order matters — admission webhook must be active before app pods are scheduled). Added the ECR pull secret creation step which was missing. Corrected the argocd login command to use NodePort (not LoadBalancer) since k3s has no cloud load balancer. Added the `--insecure` flag for self-signed cert on the lab cluster. | Mostly — ECR secret step and argocd NodePort were missing |
| Task 1 (Drift Detection Doc) | GitHub Copilot | Write a 400-word design note explaining Terraform drift vs Kubernetes configuration drift, the detection mechanism, response policy, and how Argo CD changes the drift story. | Clear explanation distinguishing the two drift types, automated `terraform plan` scheduling, Argo CD reconciliation loop description. | Enhanced the GitOps mechanism explanation to go deeper on the event-driven vs polling distinction. Added the third drift category (policy drift in Kyverno). Added the two acceptance conditions for when drift is deliberately accepted and state updated. Strengthened the SLA table with concrete response times. | Mostly — needed deeper mechanism explanation and edge cases |

---

*This log was maintained throughout the build process. Each entry documents a real interaction where AI output required verification and often correction before use.*
